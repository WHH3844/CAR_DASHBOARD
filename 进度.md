# CAR_DASHBOARD 进度

## 2026-06-05 文档补全与版本命名
- 检查当前软件架构和功能架构后，确认当前主线为 `main.c -> EcuM_Init() -> EcuM_MainLoop() -> Os_Start()`，默认运行在 FreeRTOS 拆分任务模式。
- 当前版本建议命名为 `v0.4.0-rtos-split-dashboard`，版本代号为 `RTOS Split Dashboard`；该版本代表硬件 bring-up 完成、Mini AUTOSAR-like 架构落地、FreeRTOS 拆分任务、Dashboard UI、CAN/UDS/Dem/NvM 功能闭环。
- 补全 `README.md`，同步当前版本状态、任务拆分、真实 CAN 矩阵、构建方式和新增文档入口。
- 补全 `Docs/架构版测试步骤.md`，加入 `APP_CFG_FREERTOS_SPLIT_TASKS`、拆分任务日志、TPMS 显示验证和 RTOS 回退排查方法。
- 补全 `Docs/架构落地调试记录.md` 和 `Docs/知识点梳理_HR答辩.md`，把 2026-06-03 单任务移植记录与 2026-06-04 拆分任务当前状态区分清楚。
- 新增 `Docs/软件架构与功能架构.md`，集中说明软件分层、启动链路、任务架构、数据流、诊断/存储/显示/电源等功能架构。
- 新增 `c语言语法学习.md`，按本工程真实代码整理 C 语言语法学习笔记，覆盖宏、类型、指针、数组、结构体、枚举、函数、条件编译、位操作、临界区、定点数和嵌入式常见写法。

## 2026-06-04 FreeRTOS 任务拆分落地
- 已将默认调度从单个 `EcuM` 10ms 主任务升级为可配置的拆分任务模式，开关为 `APP_CFG_FREERTOS_SPLIT_TASKS=1u`；如需回退，只需改回 `0u`。
- 新增 `CanTask/AppFastTask/DisplayTask/SensorTask/NvMTask/LoggerTask/EcuM`：通信栈仍在 `CanTask` 内串行运行，避免 `CanIf/Com/Dcm/CanTp` 并发访问；`EcuM` 保留为生命周期和关机管理。
- 已给 `Rte_Signal.c` 全局信号读写加短临界区，给 `I2c0_*` 公共 API 加 I2C0 bus mutex，给 `NvM/Dem` 周期维护和关机保存加 NvM mutex。
- `FreeRTOSConfig.h` 将 `configTOTAL_HEAP_SIZE` 从 24KB 调整为 32KB，给 7 个任务、TCB 和 mutex 留出余量。
- Keil 构建通过：`Project/Objects/codex_task_split_build.log` 显示 `0 Error(s), 0 Warning(s)`，程序规模约 `Code=34144, RO-data=1132, RW-data=340, ZI-data=34172`。

## 当前状态
- 2026-06-04：板级硬件验证已完成，电源、SDRAM、LCD、CAN、I2C、EEPROM、RTC、SHT30、TF 卡只读、按键和蜂鸣器均已跑通。
- 2026-06-04：正式 Mini AUTOSAR-like 架构主线已落地，入口为 `main.c -> EcuM_Init() -> EcuM_MainLoop() -> Os_Start()`，当前默认 FreeRTOS 单 `EcuM` 主任务调度，仍保留裸机 super loop 回退。
- 2026-06-04：CAN 矩阵 `0x321~0x328` 已补齐当前教学版开发：`0x321/0x322/0x323/0x324` 为外部输入解析，`0x325/0x326/0x327/0x328` 为仪表输出，另有简化 NM `0x440`。
- 2026-06-04：LCD 已从文本 demo 升级为定制仪表盘界面，显示左右表盘、中央车速/档位、告警灯、CAN 状态、燃油、水温、电压、时间、座舱温湿度和 TPMS。
- 2026-06-04：当前重点剩余问题是 LCD 刷新仍可能有轻微闪烁/撕裂；已做局部刷新和缓存跳过，若板上仍明显闪烁，下一步需要做双缓冲或 VSYNC 同步刷新。

## 后续记录
- 后续做成什么功能、调通什么模块、遇到什么问题，都继续往这里追加。
- 2026-05-30：补充 Test 工程组，并为 12 个测试目录加入空的测试占位 .c 文件。
- 2026-06-01：电源自锁功能已能正常开机；Keil / VSCode 下载问题已处理，可以正常烧录调试。
- 2026-06-01：`03_sdram_test` 已通过，W9825G6KH-6I SDRAM 初始化、数据线、地址线、固定模式、walking 1/0、大块填充和地址探测均通过。
- 2026-06-01：开始验证 `04_lcd_color_test`。先参考梁山派扩展板 demo 做 SPI LCD 测试，现象为背光亮但无颜色，判断该屏不是 SPI 直接刷像素的接法。
- 2026-06-01：改为 SDRAM 帧缓冲 + TLI/RGB 输出 + SPI 屏初始化流程，串口显示 `[PASS] LCD TLI + SPI init`，颜色循环日志正常，但屏幕仍只有背光。
- 2026-06-01：将 `04_lcd_color_test` 临时切到 LCD 引脚诊断模式，`TEST04_LCD_PIN_TOGGLE_DIAG = 1u`。实测 `PG7(LCD_CLK)`、`PF10(LCD_DE)`、`PC6(LCD_HSYNC)`、`PA4(LCD_VSYNC)` 等关键 LCD 线电平翻转正常，说明 MCU 到 FPC 的 GPIO 通路基本正常。
- 2026-06-01：LCD 当前结论：硬件控制线可翻转，下一步重点查屏幕真实型号、RGB 屏初始化序列、TLI 时序参数、像素时钟和同步极性。恢复彩条测试时，将 `Test/04_lcd_color_test/04_lcd_color_test.c` 中 `TEST04_LCD_PIN_TOGGLE_DIAG` 改为 `0u`。
- 2026-06-01：确认之前参考的扩展板 demo 是 SPI 小屏 demo，不适合当前 RGB 屏；新的参考工程 `RGB_800x480_DEMO` 已实测可显示。
- 2026-06-01：按 `RGB_800x480_DEMO` 移植 LCD 参数到当前工程：分辨率改为 800x480，TLI 消隐参数改为 HSYNC=10、HBP=150、HFP=15、VSYNC=10、VBP=140、VFP=40，PLLSAI 改为 192/2/3 + DIV2，DE 极性改为低有效，并启用 TLI dither。`04_lcd_color_test` 已关闭 GPIO 诊断并恢复 RGB/TLI 彩色循环测试。
- 2026-06-01：`05_can_loop_test` 已通过。USB-CAN 使用 PCAN-View 500K 正常模式，PCAN 可收到板子周期发送的标准帧 `0x321`；板子也可收到 PCAN 发送的标准帧 `0x123`，CAN TX/RX 错误计数均为 0，`CAN1_ERR_N` 保持高电平。
- 2026-06-01：下一步进入 `06_i2c_scan_test`，优先扫描 I2C0 总线地址，验证 PB6/PB7、上拉、电源、EEPROM/RTC/SHT30 焊接和地址响应。
- 2026-06-01：`06_i2c_scan_test` 已通过。I2C0 空闲电平 `SCL=H/SDA=H`，扫描到 `0x44` SHT30、`0x50~0x57` FT24C16A、`0x68` DS3231/RTC；额外扫描到 `0x38`，暂记为疑似 LCD 触摸控制器地址。
- 2026-06-01：下一步进入 `07_eeprom_test`，在 FT24C16A 的 `0x0100` 起始地址写入两组 16 字节测试模式并读回校验。
- 2026-06-01：`07_eeprom_test` 已通过。FT24C16A 在 `0x0100~0x010F` 写入 pattern A / pattern B 并读回校验成功，说明 EEPROM 基本写入、读回和写周期等待正常。
- 2026-06-01：下一步进入 `08_rtc_test`，验证 DS3231 地址响应、状态寄存器、时间读出和秒计数跳变；若 OSF 置位或时间非法，则写入默认测试时间 `2026-06-01 12:00:00`。
- 2026-06-01：`08_rtc_test` 已通过。DS3231 可读写时间，秒计数正常跳变；断电约 1 分钟后重新上电，时间从 `12:04:02` 连续走到 `12:05:27`，`status=0x09`，OSF 未再次置位，说明 CR1220 后备供电路径有效。
- 2026-06-01：下一步进入 `09_sht30_test`，读取 SHT30 温湿度，串口打印温度、湿度和原始值，并通过 CRC 校验确认数据有效。
- 2026-06-01：`09_sht30_test` 已通过。SHT30 地址 `0x44` ACK 正常，连续读取温度约 `25.7C`、湿度约 `35%RH`，对传感器哈气后湿度上升到约 `45%RH`，说明测量、CRC 校验和 I2C 6 字节读取正常。
- 2026-06-01：下一步进入 `10_tf_card_test`，验证 TF 卡座、SDIO 引脚、电源、卡识别、扇区读写和文件系统挂载。
- 2026-06-01：已准备 `10_tf_card_test` 第一版只读测试程序：SDIO 使用 `PC8~PC11/PC12/PD2`，先初始化并识别 TF 卡，再读取 0 号扇区和检查 `55 AA` 签名；暂不写卡，避免破坏卡内文件系统。
- 2026-06-01：`10_tf_card_test` 只读阶段已通过。TF 卡识别为 `SDHC/SDXC`，`RCA=0xAAAA`，`OCR=0xC0FF8000`，容量约 `7580MB`；0 号扇区读取成功，前 16 字节为有效启动代码，末尾签名为 `55 AA`。说明 TF 卡座、SDIO CMD/CLK/D0 读卡通路和基础初始化正常。
- 2026-06-01：已准备 `11_key_buzzer_test`。KEY1/KEY2/KEY3 分别使用 `PF6/PF7/PF8`，低电平按下；`BUZZER_CTRL=PF9`，高电平响。程序启动先蜂鸣器短响两声，之后按 KEY1/KEY2/KEY3 分别串口打印事件并响 1/2/3 声。
- 2026-06-01：`11_key_buzzer_test` 已通过。KEY1/KEY2/KEY3 事件识别正常，蜂鸣器启动短响和按键反馈正常，说明用户按键输入、PF9 蜂鸣器驱动和 5V 蜂鸣器供电通路正常。
- 2026-06-01：基础模块测试已覆盖电源自锁、串口/LED、SDRAM、LCD、CAN、I2C、EEPROM、RTC、SHT30、TF 卡、按键和蜂鸣器。下一步进入阶段 12 系统联调；`12.1` 上电启动流程已在前面模块测试中反复覆盖，本轮直接跳过，进入 `12.2` 软开关流程测试。
- 2026-06-01：确认软开关/长按关机流程在前面烧录、开机、关机和各模块测试中一直在使用，本轮不再单独做 `12.2` 重复测试，直接进入 `12_dashboard_demo` 整板联调。
- 2026-06-01：已准备 `12_dashboard_demo` 第一版。程序联调 LCD+SDRAM+CAN+RTC+SHT30+KEY+BUZZER：LCD 显示速度、转速、时间、温湿度、CAN 状态和报警状态；USB-CAN 推荐发送标准帧 `0x100`，8 字节数据格式为 `speed_kph_x10` 小端 + `rpm` 小端，例如 `80.0km/h / 2500rpm` 发送 `20 03 C4 09 00 00 00 00`；KEY1 进入模拟数据模式，KEY2 静音报警，KEY3 清零当前仪表值。Keil 构建通过，0 Error / 0 Warning。
- 2026-06-01：`12_dashboard_demo` 首版运行时出现 LCD 横向花屏，判断优先原因是 TLI 正在从 SDRAM 取图时，软件每 200ms 整屏擦除并重画，导致 SDRAM 读写抢占和画面撕裂。已改为固定框架只画一次，运行中 500ms 局部刷新数值/状态区域。Keil 构建通过，0 Error / 0 Warning。
- 2026-06-01：局部刷新后 LCD 花屏仍存在，进一步对比可正常显示的 `RGB_800x480_DEMO`，发现 SDRAM 控制器参数不同。已将 EXMC SDRAM 改为 DEMO 同款参数：`SDCLK=HCLK/2`、`CAS=2`、`burst read=ENABLE`、`pipeline=1`、`refresh=761`、mode register `0x0220`，用于提升 TLI framebuffer 连续读带宽和稳定性。Keil 构建通过，0 Error / 0 Warning。
- 2026-06-01：重新烧录后 `12_dashboard_demo` LCD 显示恢复正常，确认前一版花屏根因是 SDRAM 参数不适合 TLI framebuffer 连续读取，而不是 LCD 引脚、屏幕时序或 CAN/I2C/按键等外设冲突。当前 SDRAM 参数保持与 `RGB_800x480_DEMO` 一致。
- 2026-06-01：`12_dashboard_demo` 联调验证通过。LCD 仪表界面正常显示，SHT30 温湿度和 DS3231 时间正常刷新，USB-CAN 发送标准帧 `0x100` 后仪表可解析并显示 `80.0km/h / 2500rpm`；若 PCAN 只发送单帧，约 3 秒后显示 `CAN LOST` 属于信号超时保护，周期发送即可保持 `CAN OK`。至此推荐测试顺序中的电源、串口/LED、SDRAM、LCD、CAN、I2C、EEPROM、RTC、SHT30、TF 卡、按键蜂鸣器和整板 demo 均已完成，当前 PCB 板级硬件通路确认跑通。
- 2026-06-03：开始从板级测试工程切换到正式 Mini AUTOSAR-like 架构工程。已新增 `Std_Types.h` 和 `Can_Cfg/Com_Cfg/Dcm_Cfg/Dem_Cfg/NvM_Cfg/Rte_Cfg/App_Cfg`，固化 CAN ID、DID、DTC、NvM 地址和周期调度配置。
- 2026-06-03：已实现第一版 BSW/RTE/APP 主线：`CanIf + CanTrcv + PduR + CanTp + Com + Dcm + Dem + NvM + RTE + EcuM + Os`。正式数据流为 `CAN -> CanIf -> PduR -> Com -> RTE -> App_Dashboard/App_Display`，诊断数据流为 `CAN -> CanIf -> PduR -> CanTp -> Dcm -> CanTp -> CAN`。
- 2026-06-03：`main.c` 已切换为正式入口，只调用 `EcuM_Init()` 和 `EcuM_MainLoop()`；`EcuM_MainLoop()` 统一进入 `Os_Start()`，当前默认使用 FreeRTOS 单 `EcuM` 主任务，`APP_CFG_USE_FREERTOS=0` 时可回退裸机 super loop。
- 2026-06-03：新增 `Docs/架构落地调试记录.md`、`Docs/知识点梳理_HR答辩.md` 和 `Docs/架构版测试步骤.md`，用于记录本轮架构迁移过程、测试方法、关键问题和面试答辩知识点。
- 2026-06-03：开始从板级测试阶段切换到正式 Mini AUTOSAR-like 架构落地。`main.c` 已改为 `EcuM_Init()` + `EcuM_MainLoop()`，不再直接运行 `12_dashboard_demo`。
- 2026-06-03：补齐第一版配置基线：`Std_Types.h`、`Can_Cfg.h`、`Com_Cfg.h`、`Dcm_Cfg.h`、`Dem_Cfg.h`、`NvM_Cfg.h`、`Rte_Cfg.h`、`App_Cfg.h`。CAN ID 已切到矩阵中的 `0x321~0x328`、`0x440~0x441`、`0x700/0x708/0x7DF`。
- 2026-06-03：实现 BSW/RTE/APP 主线：`EcuM/Os/LogM/Crc/NvM/Dem/Dcm/CanIf/CanTrcv/CanSM/PduR/CanTp/Com/RTE/LcdIf/SdramIf/SdramMgr/BacklightIf/App_Dashboard/App_Display/App_Key/App_Power/App_Sensor`。
- 2026-06-03：实现教学级 UDS 单帧诊断闭环，支持 `0x10`、`0x22`、`0x19`、`0x14`、`0x3E`；DTC 状态和启动次数通过 FT24C16A EEPROM + CRC16 保存。
- 2026-06-03：Keil 命令行构建完成，日志显示已编译/链接并生成 hex，程序规模约 `Code=20500, RO-data=1020, RW-data=152, ZI-data=1144`。下一步需要烧录板上验证正式架构主线：LCD 主界面、`0x321` CAN 输入、`0x325/0x326/0x440` 输出、`0x700->0x708` 诊断响应、NvM/Dem 掉电保持。
- 2026-06-03：已从 `D:\MCU\FreeRTOSv202212.01\FreeRTOS\Source` 移植 FreeRTOS 到 `Os/FreeRTOS/Source`，选择 Cortex-M4F `portable/RVDS/ARM_CM4F` 端口和 `heap_4.c`；新增 `Config/FreeRTOSConfig.h`，Keil 工程已加入内核源码和 include 路径，构建结果为 `0 Error(s), 0 Warning(s)`，程序规模约 `Code=24556, RO-data=1020, RW-data=264, ZI-data=25920`。
- 2026-06-04：完善代码注释和架构说明文档，补充 `CanTrcv/CanSM/CanTp/Com/RTE/EcuM/Os/App_Display` 等关键模块注释，便于答辩时说明分层职责和调用路径。
- 2026-06-04：完成 PCAN-VIEW 对 `0x321` 的有效性验证。发现 `0x321` 周期设置为 `500ms` 时可能刚好触发 `CAN_CFG_EMS_POWERTRAIN_TIMEOUT_MS=500ms` 边界，导致 `0x325 Byte5` 偶发保持 `00`；改为 `50ms` 后 `0x325 Byte5=01`、`0x326 Byte2=01`，说明动力 CAN 当前有效。
- 2026-06-04：明确 PCAN 中 `0x326 Byte0=01` 表示历史 confirmed DTC 数量，不等于当前仍故障；当前有效性应看 `0x326 Byte2`，历史 DTC 可通过 UDS `0x14` 清除。
- 2026-06-04：补齐 CAN 报文开发：`0x323 TPMS_Status_1000ms` 解析四轮胎压/胎温，`0x324 IHU_CGW_Config_500ms` 解析主题、语言、单位制、背光、音量、续航和外部时间，`0x327 ICM_UserInput_Event` 按键事件触发发送，`0x328 ICM_LogStatus_1000ms` 周期上报日志状态和运行秒数。
- 2026-06-04：保留 `0x325/0x326` 已经通过 PCAN 验证的教学版打包方式：`0x325` 回传车速、转速、报警/静音/模拟模式、动力 CAN 有效位和 `warning_flags`；`0x326` 回传 confirmed DTC 数量、诊断会话和动力 CAN 有效位。后续若要完全贴合矩阵，可单独重构发送字段。
- 2026-06-04：梳理 CAN 报文方向：`0x321/0x322/0x323/0x324` 是外部 ECU 或 PCAN 输入给仪表的报文，板子不会主动发；`0x325/0x326/0x328` 是仪表周期输出，`0x327` 是按键事件输出，`0x440` 是当前额外简化 NM。
- 2026-06-04：处理档位显示问题。`0x322 Byte0=0x09` 按矩阵解析为 `N`，`0x0D` 才是 `D`；显示层为演示效果增加兜底：若车速已经大于 0 但 `0x322` 仍报 `N`，LCD 显示 `D`，其它 `P/R/S/M` 仍按报文显示。
- 2026-06-04：升级 `App_Display` 仪表盘界面：左侧 RPM 表盘、右侧车速表盘、中央大号车速和档位、顶部 `CAN OK/CAN LOST/SIM MODE` 与 MIL/ABS/AIR/BRK/TPMS 提示，底部显示燃油、水温、电池电压、RTC 时间、座舱温湿度和四轮胎压。
- 2026-06-04：新增 `LcdIf_DrawLine()`，用于绘制表盘弧线、刻度和指针；显示层根据 RTE 数据定制仪表内容，不再只是旧版 `SPEED/RPM/TIME/TEMP/HUM` 文本界面。
- 2026-06-04：针对 LCD 闪烁做第一轮优化：移除 `App_Display_MainFunction()` 每周期整屏 `LcdIf_Clear()`，改为 LCD ready 后只清一次，后续局部刷新顶部、表盘、中央车速、档位和底部区域。
- 2026-06-04：针对 LCD 仍闪烁做第二轮优化：左右表盘不再整块填充背景，改成擦旧指针、补刻度、画新指针；顶部状态、中央车速、档位和底部信息增加缓存，值不变时跳过重画；底部时间跳秒只刷新 `TIME` 小区域。
- 2026-06-04：修正显示布局细节：收紧 `CABIN 29.46 C 44.52 %` 中单位与数字的距离，缩小并居中速度表刻度数字，避免 `100/120/140` 在表盘顶部挤在一起；调整绘制顺序，中央速度先画、左右表盘后补，避免中央清除区域擦掉表盘内侧弧线。
- 2026-06-04：增加 RTC 自动校准逻辑。`App_Sensor_Init()` 中若 DS3231 时间无效、OSF 掉电标志置位，或 RTC 时间明显早于本次编译时间，则自动写入本次编译时的北京时间；显示层时间格式改为 `HH:MM:SS`。
- 2026-06-04：讨论 FreeRTOS 任务拆分方案。当前仍使用单 `EcuM` 主任务承载原 10ms 调度，优点是避免 RTE/LCD/I2C/EEPROM 并发访问；若后续拆成 `CanTask/AppFastTask/DisplayTask/SensorTask/NvMTask/LoggerTask`，需要先给 RTE、I2C、NvM 等共享资源加临界区或 mutex。
- 2026-06-04：最新 Keil 构建通过，`Project/Objects/codex_display_less_flicker_build.log` 显示 `0 Error(s), 0 Warning(s)`，程序规模约 `Code=30344, RO-data=1132, RW-data=332, ZI-data=25980`。
